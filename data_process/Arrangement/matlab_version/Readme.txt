run "main.m" you can get all the result
C57_all_RUs.mat contains all the syllables of C57 mouse strain
segment_whole_dataset.m: divide the dataset to inlier dataset and outlier dataset which are stored in segmented_data.mat
denoise_image.m: denoise and normalize the raw syllable image
sparse_regression.m: solve sparse regression with lasso
look_center_fun.m: get center and syllables of each cluster

other .m files achieve some simple function 
